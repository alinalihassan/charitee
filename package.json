{
  "name": "charitee",
  "version": "0.1.0",
  "description": "Charitee",
  "license": "UNLICENSED",
  "author": "Alin Ali Hassan",
  "private": true,
  "scripts": {
    "lint": "lerna run lint --parallel --no-bail",
    "lint:fix": "lerna run lint:fix --parallel --no-bail",
    "build": "lerna run build --stream",
    "start": "lerna run start --stream",
    "start-backend": "lerna run start:prod --stream --scope charitee-backend",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "@types/node": "^16.4.6",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-prettier": "^4.0.0",
    "gts": "^3.1.0",
    "lerna": "^4.0.0",
    "typescript": "^4.0.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
